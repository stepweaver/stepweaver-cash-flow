{
  "buildCommand": "pnpm run build:ci",
  "installCommand": "pnpm install --frozen-lockfile --prefer-offline",
  "build": {
    "env": {
      "NODE_ENV": "production"
    }
  },
  "functions": {
    "app/api/**/*.js": {
      "maxDuration": 30
    }
  }
}
